// Main Logic for Interactions (Hybrid Approach)
console.log("SciEquip Loaded");

// Basic Modal Toggler
function toggleModal(id) {
    const el = document.getElementById(id);
    if (el) el.classList.toggle('hidden');
}

// Add more vanilla JS interactivity here for tabs, etc.